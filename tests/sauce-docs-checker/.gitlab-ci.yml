image: node:latest

stages:
  - test

cache:
  paths:
    - node_modules/

start:
  stage: test
  script:
    - npm install
    - echo $CI_JOB_URL
    - npm start -- $CI_JOB_URL
  tags: [gke, k8s, multi-runner, tools, us-west1]
