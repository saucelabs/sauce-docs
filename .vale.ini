StylesPath = styles

# suggestion, warning, or error
MinAlertLevel = suggestion

[*.md]
BasedOnStyles = sauce
BlockIgnores = ```[a-z]*[\s\S]*?\n```
TokenIgnores = \(\/.*?\), \
               (\d+\.\d+\.\d+\.\d+), \
               {%.*?%}, \
               {{.*?}}, \
               (.|\/|-)?github(\/|-)?, \
               (<http[^\n]+>+?), \
               (```[a-z]*[\s\S]*?\n```), \
               (\"+[^\n$]+\"+), \
               (\'+[^\n$]+\'+)
